prefix := @prefix@
exec_prefix := @exec_prefix@
IVC_LIB := @libdir@
IVC_INC := @includedir@

all: $(BINARIES)

clean::
	rm -f $(BINARIES) *.hi *.o

%: %.hs
	halvm-ghc --make -o $@ $^

%: %.c
	gcc -o $@ $^ -I$(IVC_INC) -lxenctrl -lcrypto -lxenstore $(IVC_LIB)/libIVC.a
